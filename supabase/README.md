# Supabase backend

This directory will contain versioned migrations, Row Level Security policies and tests, private evidence-storage configuration, generated database types, and any narrow Edge Functions needed for trusted ingestion or notification orchestration.

No credentials are required before Milestone 7. Heavy video inference will not run in Edge Functions.

