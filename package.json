{
  "name": "context-aware-security-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @security-ai/contracts",
    "check": "npm run check --workspace @security-ai/contracts",
    "test": "npm run test --workspace @security-ai/contracts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
