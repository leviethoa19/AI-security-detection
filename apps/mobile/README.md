# Mobile client

The Expo Router client will be implemented after the secure backend vertical slice. Its planned scope is authentication, arm/disarm, camera and zone configuration, incident list/detail, private evidence preview, reason codes, risk timeline, and feedback.

Deferring the UI scaffold keeps the first milestones focused on validated event semantics and avoids locking a client to an unstable contract.

